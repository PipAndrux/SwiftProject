import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 10){
            Text("Lissone, MB")
            Text("Where do")
                .font(.system(size: 48, weight: .bold))
            Text("we go today?")
                .font(.system(size: 48, weight: .bold))
            
            Image(systemName: "")
            Spacer()
        }.padding(.top, 20)
        
        
        
    }
}
